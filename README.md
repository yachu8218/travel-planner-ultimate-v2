# Travel Planner Ultimate v2.9.4

## 本次修正

v2.9.3 的小插畫仍然是一個獨立排版列，因此會：

- 位置偏下
- 右側行程卡跟著多出空白

v2.9.4 改為：

- 小插畫附著在前一個行程的時間軸區域
- 使用前一張行程高度、時間框高度與間距計算正中央
- 小插畫不參與右側卡片排版
- 右側行程卡不再保留任何多餘空白

首頁 7 天天氣與既有 API 設定全部保留。

## 更新方式

解壓縮後覆蓋 GitHub 原檔案並 Commit，Cloudflare Pages 會自動部署。
