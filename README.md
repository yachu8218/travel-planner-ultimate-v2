# Travel Planner Ultimate v2.3.4 — UI Refinement

## 本次調整

- 所有主要卡片取消漸層，改為單一柔和色系
- Header 改為單一底色，不再有斷點
- 卡片、按鈕、圖示與陰影統一
- 快捷金額自動換行
- 點餐數量表不再撐寬
- 全站禁止橫向捲動
- 底部增加安全留白，可完整看到最後一張卡片

## 更新方式

解壓縮後，全選內容，上傳至原 GitHub Repository 覆蓋同名檔案並 Commit。Cloudflare Pages 會自動部署。

本版不需要新增 API Key。
